---

- name: test if terraform_version is set correctly
  assert:
    that:
      - terraform_version is defined
    quiet: yes

- name: test if terraform_destination is set correctly
  assert:
    that:
      - terraform_destination is defined
    quiet: yes
